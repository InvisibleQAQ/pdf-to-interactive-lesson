# OpenAI-compatible API (Required)
# Works with any OpenAI-compatible endpoint (vLLM, Ollama, LiteLLM, OpenRouter, Together AI, etc.)
OPENAI_BASE_URL=https://your-provider.com/v1
OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=your-model-name
# Optional: separate model for grading (defaults to OPENAI_MODEL)
OPENAI_GRADER_MODEL=your-grader-model-name

# Together AI API Key (Optional — legacy fallback, used when OPENAI_* vars are not set)
# TOGETHER_API_KEY=your_together_api_key_here

# Anthropic API Key (Optional — only needed if you explicitly set a judge model with the anthropic/ prefix)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Neon Database URL (Required)
# Get this from Neon Dashboard > Connection Details
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=your_neon_database_url_here

# Vercel Blob Storage Token (Required for web interface)
# Used for storing uploaded PDFs
# Get this from Vercel Dashboard > Storage > Blob > Connect
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Upstash Redis (Required for web generation queue and rate limiting)
# Get these from Upstash Redis > REST API
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here

# Public app URL (Required for canonical URLs and social previews)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Generation concurrency (Optional, defaults to 2)
GENERATION_MAX_CONCURRENT=2
